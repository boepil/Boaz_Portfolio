{
    "name": "art-portfolio",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "dev": "npm run generate && vite",
        "build": "npm run generate && vite build",
        "preview": "vite preview",
        "generate": "node scripts/process-assets.js"
    },
    "devDependencies": {
        "fs-extra": "^11.1.0",
        "vite": "^5.0.0"
    },
    "dependencies": {
        "exifr": "^7.1.3"
    }
}
